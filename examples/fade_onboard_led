# Example fading the onboard LED using PWM

from time import sleep
from pico import LED

led = LED()

while True:
    for b in range(101):
        led.brightness = b
        sleep(0.01)
    for b in range(100, 0, -1):
        led.brightness = b
        sleep(0.01)
    
